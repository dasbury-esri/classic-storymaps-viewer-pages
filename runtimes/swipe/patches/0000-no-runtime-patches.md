# 0000 No Runtime Patches (Baseline)

Swipe import baseline currently requires no upstream source patch files.

Any future runtime behavior adjustments must be captured as numbered patch files and referenced in `runtimes/swipe/runtime-manifest.json`.
